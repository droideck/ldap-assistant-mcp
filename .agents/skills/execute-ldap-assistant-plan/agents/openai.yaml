interface:
  display_name: "Execute LDAP Assistant Plan"
  short_description: "Run one audited implementation task safely"
  default_prompt: "Use $execute-ldap-assistant-plan to execute exactly <TASK_ID> with externally admitted contract receipt <ORIGINAL_ADMISSION_CONTRACT_SHA256>, fail-before evidence, required gates, independent review, and no work on later tasks."
policy:
  allow_implicit_invocation: false
